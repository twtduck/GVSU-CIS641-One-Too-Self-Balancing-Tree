<UserControl x:Class="OneTooCalendar.QuarterHourPeriodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OneTooCalendar"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance local:QuarterHourPeriodViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1" />
            <RowDefinition Height="11" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Visibility="{Binding SeparatorVisibility}" BorderBrush="{x:Static local:ThemeHelper.CalendarDivider}" BorderThickness="0, 1, 0, 0" />
    </Grid>
</UserControl>
